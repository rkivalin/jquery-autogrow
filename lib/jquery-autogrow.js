// Generated by CoffeeScript 1.3.1

(function($) {
  var inherit;
  inherit = ['font-family', 'font-size', 'font-variant', 'font-weight', 'font-stretch', 'letter-spacing'];
  return $.fn.autoGrow = function(options) {
    options = $.extend({
      maxWidth: 1000,
      minWidth: 0,
      comfortZone: 70
    }, options);
    return this.each(function() {
      var check, input, maxWidth, minWidth, prop, styles, testSubject, _i, _len;
      maxWidth = options.maxWidth;
      minWidth = options.minWidth || $(this).width();
      input = $(this);
      styles = {
        position: 'absolute',
        top: -9999,
        left: -9999,
        width: 'auto',
        visibility: 'hidden'
      };
      for (_i = 0, _len = inherit.length; _i < _len; _i++) {
        prop = inherit[_i];
        styles[prop] = input.css(prop);
      }
      testSubject = $('<pre/>').css(styles);
      check = function() {
        var newWidth;
        testSubject.text(input.val());
        newWidth = testSubject.width() + options.comfortZone;
        if (newWidth > maxWidth) {
          newWidth = maxWidth;
        }
        if (newWidth < minWidth) {
          newWidth = minWidth;
        }
        return input.width(newWidth);
      };
      testSubject.insertAfter(input);
      return $(this).bind('input', check);
    });
  };
})(typeof Zepto !== "undefined" && Zepto !== null ? Zepto : jQuery);
